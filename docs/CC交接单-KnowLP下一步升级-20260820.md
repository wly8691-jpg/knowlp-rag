# CC 交接单：KnowLP 下一步升级排期

日期：2026-08-20
交接方：CTO（wly10）→ CC
关联仓库：`C:\Users\wly10\knowlp-rag-local`（发布源）/ `github.com/wly8691-jpg/knowlp-rag`
数据层：`Documents\Obsidian Vault\系统\knowlp-graph\`（dual_graph.json / vector_index.json / config.yaml）

---

## 一、现状基线（开工前先确认）

- KnowLP 版本：3.0.7-rc.0（MCP + native Cordis plugin 双壳双宿主已实现）
- 衰减函数：8/15 上线，2532 条边回填，测试 11/11，P@5 0.27→0.28，**观察期至 ~8/29**
- 五工具/四引擎扇出检索/自动入库/反馈闭环三件套：已实现
- 竞争护栏：decree 体系 + 衰减生命周期 + Obsidian 可见可审计（三条独门护城河）

## 二、任务清单（按时间序）

### T1：衰减观察收尾 → BCM 二期（~8/29 截止）

- [ ] 观察期内**只读不写**：不动存储层、不重算权重、不加新边（防污染衰减样本）
- [ ] 8/29 汇总衰减观察日志：衰减曲线、P@5 走势、误报率（两周目标）
- [ ] 观察数据正常 → 上 BCM 二期（具体范围见 docs 既定计划）
- [ ] 观察数据异常 → 停下，先出根因报告再动

### T2：反馈闭环升级——偏好学习替代手工调权重（设计单已落，参照焊接）

背景：8/18 已拍板——把用户纠正记录当**成对偏好样本**，用偏好学习自动学习图权重，替代手工规则调权重。

**论文参照（CC 必读）**：《Efficient Preference-Based RL: Randomized Exploration Meets Experimental Design》（NeurIPS 2025, EPFL SYCAMORE, **arXiv:2506.09508**）——清华×港大 DoF 评审 #005 解析（3 张图已随对话给过）。核心：随机化探索（奖励参数后验采样替代乐观探索）+ Lazy Update（冻结期查询并行）+ Greedy D-Optimal（选信息量最大的轨迹对查询）。定理数值以论文 §3-4 为准。

**设计单已落：`docs/设计单-T2反馈闭环偏好学习-20260821.md`** ——同构映射（纠正记录=偏好对 / 图权重=奖励参数 / 四引擎=RL oracle）、三机制（后验采样更新 / D-Optimal 主动查询 / Lazy Update 攒批）、多端收集纠正记录（收集端跨机无状态、更新端单点归位）、Line2 接口预留、红线、验收六项。

- [ ] 先读论文 §3 算法伪码 + §4 理论，再读设计单，按单焊接（先单后码已完成，直接进码）
- [ ] 样本构造（纠正记录→pair）、损失/更新函数选型、回滚机制——按设计单第二节
- [ ] 多端收集纠正记录：台式机分身（:8642）产生纠正事件上送本机，本机攒批更新——按设计单第三节
- [ ] 设计单评审通过后再实现（评审人：CTO/AI）
- [ ] decree 体系挂触发词：`反馈闭环升级 / 偏好强化 / reward model`
- [ ] Line2 预留：Loop 判定器升级为轻量 reward model（本单只留接口，不实现）
- [ ] 验收：偏好样本注入后 P@5 不降 + 反馈日志可审计

### T3：3.0.7-rc.0 → 正式版发布

- [ ] rc 观察期无回归 → 发正式版（npm Trusted Publishing 通道已通，GitHub 2FA 已配）
- [ ] 发布前跑完整回归：五工具实测 + MCP 握手 + Cordis 双宿主

### T4：v5-local-brain 完全本地化（现可启动）

- [ ] 主档：RTX Spark 128GB + Ollama Qwen3-235B-A22B，完全离线
- [ ] 降档：4090 + qwen3.8:27b（Q5_K_M 量化 + 64K 上下文）
- [ ] 验收：断网环境下检索/入库/衰减全链路可用

### T5：Loop 前置——KnowLP 供货部分（现可启动）

**T5.0 记忆轨迹记录（先于冷启动/session 恢复实现——恢复依据）**

> 背景（8/21 会议收获）：冷启动协议是"醒来读日记的哪几页"，**轨迹就是那本日记**。没有轨迹，恢复只能全量装/猜。OpenViking 检索轨迹可视化、OpenHuman root-cause reports 同向验证。

- [ ] session 级 turn 事件日志：输入摘要 / 注入了哪些记忆块（Top-K 名单+权重）/ 动作 / 结果 / 反馈
- [ ] 轨迹可审计：vault 可见（护城河③不破）+ 判定器事后打分的输入
- [ ] 验收：一次会话结束 → 轨迹文件落盘 → 可回放检索路径

- [ ] 多租户隔离（命名空间级）
- [ ] session 恢复强化（依赖 T5.0 轨迹：按轨迹选择恢复哪几块人设/场景切片，而非全量）
- 注：版本化 rollout、判定器低置信升级属 Loop 编排层，**不在本单**

### T6：分层记忆（L0/L1/L2 按需加载，8/21 会议对标 OpenViking 后增补）

> 背景：字节 OpenViking（31K★）——同一记忆存三层：L0 摘要 / L1 概览 / L2 全文，先装 L0/L1、需要再取 L2。LoCoMo10 实证：**token 降 83%、完成率 +49%**。与冷启动正交：冷启动管"装哪几块"（横切），分层管"每块给多少"（纵切）。
> 纪律：抄思想不搬实现（只抄 L0/L1/L2 粒度+目录递归检索+轨迹可视化）；OpenViking AGPLv3，**不进商业交付**；KnowLP 差异化（双图+衰减+decree+OB 可见）不丢。

- [ ] 存储格式：每条记忆维护 L0/L1/L2 三层（**不动衰减观察期存量样本，新写入先起三层**）
- [ ] 检索路径：先向量定位块 → 块内 L0/L1 注入 → 需要时按需取 L2
- [ ] 与冷启动协议合并验收：冷启动装块时只装 L0/L1，任务需要再取 L2
- [ ] 验收：注入 token 对比（分层 vs 全量）有实测降幅 + P@5 不降 + 轨迹可审计

- [ ] v5-local-brain 语境：本地 4090 64K 上下文预算紧张，分层加载收益最大（与 T4 并行或紧随）

### T7：NeuroPath 路径追踪参照——第五检索引擎+轨迹素材（8/21 论文拆解增补，NeurIPS 2025·电子科技大学）

> 背景：NeuroPath（github.com/KennyCai/NeuroPath，已开源）——LLM 从种子节点逐跳做语义路径追踪（保留/扩展/剪枝，top-k=30），"中间推理链+原查询"拼接做二阶段检索。实证：R@2 +16.3% / token -22.8% vs 全部基线。与 T5.0 轨迹、T2 偏好学习同赛道。
> 纪律：抄思想不搬实现；不引入 LLM 静态建索引层（KnowLP 双图已有）；红线不动。

- [ ] 设计单（先单后码）：**第五检索引擎** = 语义路径追踪（四引擎扇出 + 路径引擎），LLM 每跳输出"扩展需求"作剪枝依据
- [ ] T5.0 轨迹格式吸收：轨迹记录纳入**每跳推理链**（q' = 原查询 + 推理链 + 扩展需求 的检索/恢复形态）
- [ ] 与 T2 偏好学习的关系：路径选择可产偏好样本（用户确认"走对路径"= 正样本）
- [ ] 验收：P@5 不降 + 轨迹可回放检索路径 + token 对比（路径引擎 vs 纯扇出）

### T8：memswap——记忆后端热切换（8/14 技术跟踪定案，设计层 → 落地）

> 背景：DSH 原生热拔插（ctx.plugin 挂载 / fiber.dispose 卸载），KnowLP 按**独立插件**做；8/16 竞品收口已将 memswap 叙事**收窄为"记忆后端可替换"**（热挂/卸插件探针层被 dsh-evolve 抢先，核心层无人碰——后端抽象+数据迁移+换脑不换记忆仍是独占空位）。
> 哲学：换脑不换记忆——记忆主权不绑定任何一家模型/宿主。

- [ ] KnowLP 插件独立化：声明依赖 = 索引路径；卸载 = 卸内存索引**不动文件**；不碰记忆层接口
- [ ] 记忆后端抽象层：统一接口（检索/写入/衰减），后端可替换（默认本地文件+向量库，预留 Mem0/Chroma 等）
- [ ] 验收：换模型/换宿主（DSH↔Claude Code）后记忆完整迁移 + 卸载重装索引可重建
- [ ] 红线：衰减观察期（8/29 前）存储层零接触；三条护城河（decree/衰减/OB 可见）不破

## 三、红线（不可越）

1. **衰减观察期（~8/29 前）存储层零接触**——只读不写，任何数据变更需先过 CTO
2. **KnowLP 不碰沙箱/能力索引赛道**——dsh-worlds 沙箱持久化（Loop §8.6）、dsh-capability-index 能力索引（Loop §8.7）归 Loop 编排层，KnowLP 只做记忆层
3. **三条护城河只增不减**：decree 体系、衰减生命周期、Obsidian 可见可审计——升级任何一处都要保留这三条可见性
4. 改码前先读原始材料；自改自断言不算数

## 四、验收标准（通用）

- 官方通道四步：干净装 → 启动 → 注册 → 实调有返回
- 断言 PASS + 数据校验闭环（P@5 / 命中数 / 日志可审计）
- 交付物：代码 + 测试 + 变更说明（vault 可见）

## 五、排期

| 任务 | 窗口 | 状态 |
|---|---|---|
| T1 衰减收尾+BCM二期 | 8/20-8/29 | 待启动 |
| T2 偏好学习设计单 | 8/20-8/24（先单后码） | 待启动 |
| T3 正式版发布 | 8/29 后 | 待 rc 观察 |
| T4 本地化 | 现可启动 | 待启动 |
| T5 Loop 前置供货（含 T5.0 记忆轨迹） | 现可启动 | 待启动 |
| T6 分层记忆（L0/L1/L2） | 与 T4 并行或紧随 | 待启动 |
| T7 NeuroPath 路径参照（第五引擎+轨迹素材） | 与 T5/T6 并行评估 | 待启动 |
| T8 memswap 记忆后端热切换 | 设计层已有，现可启动 | 待启动 |

优先级：T1 观察期不动手 → T2 设计单并行（纯增量不碰存储）→ T3 随 rc 观察自然触发 → **T5.0 轨迹先行**（冷启动/session 恢复的恢复依据）→ T6 分层与 T4 本地化并行。
